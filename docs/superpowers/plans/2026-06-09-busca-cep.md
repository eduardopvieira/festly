# Busca por CEP — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Autopreencher endereço a partir do CEP (via ViaCEP) no modal de agendamento, no cadastro/edição de serviço e no checkout; persistir `cep`+`estado` no serviço.

**Architecture:** Abordagem A — busca feita no frontend direto no ViaCEP através de um `cepService` + hook `useBuscaCep` + util `formatCep`, reaproveitados nos 3 formulários. Backend muda apenas para persistir `cep`/`estado` no `Servico`.

**Tech Stack:** Spring Boot (Java 17) + Flyway no backend; React 19 + Vite + react-hook-form + Zod no frontend.

**Nota:** Sem testes automatizados nesta feature (decisão do usuário). Verificação = compilar/lintar/rodar e checagem manual. Commits a cada tarefa.

---

## Estrutura de arquivos

**Criar:**
- `festly-backend/src/main/resources/db/migration/V18__add_cep_estado_servicos.sql`
- `festly-frontend/src/lib/formatCep.js`
- `festly-frontend/src/services/cepService.js`
- `festly-frontend/src/hooks/useBuscaCep.js`

**Modificar:**
- `festly-backend/src/main/java/com/projeto/festly/entity/Servico.java`
- `festly-backend/src/main/java/com/projeto/festly/dto/ServicoRequest.java`
- `festly-backend/src/main/java/com/projeto/festly/dto/ServicoResponse.java`
- `festly-backend/src/main/java/com/projeto/festly/service/ServicoService.java`
- `festly-frontend/src/components/ServicoForm.jsx`
- `festly-frontend/src/pages/EditarServico.jsx`
- `festly-frontend/src/components/SolicitarAgendamentoModal.jsx`
- `festly-frontend/src/pages/Checkout.jsx`

---

## Task 1: Backend — entidade Servico + migration

**Files:**
- Modify: `festly-backend/src/main/java/com/projeto/festly/entity/Servico.java`
- Create: `festly-backend/src/main/resources/db/migration/V18__add_cep_estado_servicos.sql`

- [ ] **Step 1: Adicionar colunas na entidade**

Em `Servico.java`, logo após o campo `cidade` (linha ~44):

```java
    @Column(nullable = false)
    private String cidade;

    @Column(length = 9)
    private String cep;

    @Column(length = 2)
    private String estado;
```

- [ ] **Step 2: Criar a migration**

`V18__add_cep_estado_servicos.sql`:

```sql
ALTER TABLE servicos
    ADD COLUMN cep VARCHAR(9),
    ADD COLUMN estado VARCHAR(2);
```

- [ ] **Step 3: Compilar o backend**

Run: `cd festly-backend; ./mvnw -q compile`
Expected: BUILD SUCCESS, sem erros de compilação.

- [ ] **Step 4: Commit**

```bash
git add festly-backend/src/main/java/com/projeto/festly/entity/Servico.java festly-backend/src/main/resources/db/migration/V18__add_cep_estado_servicos.sql
git commit -m "feat: adiciona cep e estado na entidade Servico"
```

---

## Task 2: Backend — DTOs e mapeamento

**Files:**
- Modify: `festly-backend/src/main/java/com/projeto/festly/dto/ServicoRequest.java`
- Modify: `festly-backend/src/main/java/com/projeto/festly/dto/ServicoResponse.java`
- Modify: `festly-backend/src/main/java/com/projeto/festly/service/ServicoService.java`

- [ ] **Step 1: Adicionar campos no ServicoRequest**

Em `ServicoRequest.java`, após o campo `cidade`, adicionar (opcionais — validação de formato só se preenchidos):

```java
    @Pattern(regexp = "\\d{5}-?\\d{3}", message = "CEP inválido")
    private String cep;

    @Pattern(regexp = "[A-Za-z]{2}", message = "UF inválida")
    private String estado;
```

(`@Pattern` já está disponível via `import jakarta.validation.constraints.*;`)

- [ ] **Step 2: Adicionar campos no ServicoResponse**

Em `ServicoResponse.java`, adicionar os atributos após `private String cidade;`:

```java
    private String cidade;
    private String cep;
    private String estado;
```

E, no método `from(...)`, após `response.setCidade(servico.getCidade());`:

```java
        response.setCidade(servico.getCidade());
        response.setCep(servico.getCep());
        response.setEstado(servico.getEstado());
```

- [ ] **Step 3: Preencher os campos na criação (builder)**

Em `ServicoService.java`, no `Servico.builder()` (~linha 36-41), após `.cidade(request.getCidade())`:

```java
                .cidade(request.getCidade())
                .cep(request.getCep())
                .estado(request.getEstado())
```

- [ ] **Step 4: Preencher os campos na edição (setters)**

Em `ServicoService.java`, após `servico.setCidade(request.getCidade());` (~linha 104):

```java
        servico.setCidade(request.getCidade());
        servico.setCep(request.getCep());
        servico.setEstado(request.getEstado());
```

- [ ] **Step 5: Compilar o backend**

Run: `cd festly-backend; ./mvnw -q compile`
Expected: BUILD SUCCESS.

- [ ] **Step 6: Commit**

```bash
git add festly-backend/src/main/java/com/projeto/festly/dto/ServicoRequest.java festly-backend/src/main/java/com/projeto/festly/dto/ServicoResponse.java festly-backend/src/main/java/com/projeto/festly/service/ServicoService.java
git commit -m "feat: persiste cep e estado do servico nos DTOs e service"
```

---

## Task 3: Frontend — util formatCep

**Files:**
- Create: `festly-frontend/src/lib/formatCep.js`

- [ ] **Step 1: Criar o util**

`formatCep.js`:

```js
/** Aplica máscara 00000-000, cortando em 8 dígitos. */
export function formatCep(valor) {
  const d = (valor ?? '').replace(/\D/g, '').slice(0, 8);
  if (d.length <= 5) return d;
  return `${d.slice(0, 5)}-${d.slice(5)}`;
}
```

- [ ] **Step 2: Commit**

```bash
git add festly-frontend/src/lib/formatCep.js
git commit -m "feat: util formatCep para mascara de CEP"
```

---

## Task 4: Frontend — cepService (ViaCEP)

**Files:**
- Create: `festly-frontend/src/services/cepService.js`

- [ ] **Step 1: Criar o serviço**

`cepService.js`:

```js
export class CepInvalidoError extends Error {
  constructor() {
    super('CEP deve ter 8 dígitos.');
    this.name = 'CepInvalidoError';
  }
}

export class CepNaoEncontradoError extends Error {
  constructor() {
    super('CEP não encontrado.');
    this.name = 'CepNaoEncontradoError';
  }
}

/** Remove tudo que não for dígito. */
export function soDigitos(cep) {
  return (cep ?? '').replace(/\D/g, '');
}

/**
 * Busca um CEP no ViaCEP e normaliza o resultado.
 * @returns {Promise<{cep:string, rua:string, bairro:string, cidade:string, estado:string}>}
 */
export async function buscarCep(cep) {
  const digitos = soDigitos(cep);
  if (digitos.length !== 8) throw new CepInvalidoError();

  // Gancho de fallback futuro (BrasilAPI) poderia entrar aqui.
  const resp = await fetch(`https://viacep.com.br/ws/${digitos}/json/`);
  if (!resp.ok) throw new Error('Falha ao consultar o CEP.');

  const data = await resp.json();
  if (data.erro) throw new CepNaoEncontradoError();

  return {
    cep: data.cep ?? '',
    rua: data.logradouro ?? '',
    bairro: data.bairro ?? '',
    cidade: data.localidade ?? '',
    estado: data.uf ?? '',
  };
}
```

- [ ] **Step 2: Commit**

```bash
git add festly-frontend/src/services/cepService.js
git commit -m "feat: cepService consulta ViaCEP"
```

---

## Task 5: Frontend — hook useBuscaCep

**Files:**
- Create: `festly-frontend/src/hooks/useBuscaCep.js`

- [ ] **Step 1: Criar o hook**

`useBuscaCep.js`:

```js
import { useRef, useState, useCallback } from 'react';
import { buscarCep, soDigitos, CepNaoEncontradoError } from '@/services/cepService';

/**
 * Encapsula a busca de CEP com estado de loading/erro e proteção contra
 * respostas obsoletas. O form passa um onSuccess(endereco) para preencher
 * seus próprios campos.
 */
export function useBuscaCep() {
  const [loading, setLoading] = useState(false);
  const [erro, setErro] = useState('');
  const versaoRef = useRef(0);

  const buscar = useCallback(async (cep, onSuccess) => {
    const digitos = soDigitos(cep);
    if (digitos.length !== 8) return; // só dispara com 8 dígitos

    const versao = ++versaoRef.current;
    setLoading(true);
    setErro('');
    try {
      const endereco = await buscarCep(digitos);
      if (versao !== versaoRef.current) return; // resposta obsoleta
      onSuccess(endereco);
    } catch (e) {
      if (versao !== versaoRef.current) return;
      setErro(
        e instanceof CepNaoEncontradoError
          ? 'CEP não encontrado.'
          : 'Não foi possível buscar o CEP. Preencha manualmente.'
      );
    } finally {
      if (versao === versaoRef.current) setLoading(false);
    }
  }, []);

  return { buscar, loading, erro };
}
```

- [ ] **Step 2: Commit**

```bash
git add festly-frontend/src/hooks/useBuscaCep.js
git commit -m "feat: hook useBuscaCep com loading/erro e anti-race"
```

---

## Task 6: Frontend — ServicoForm (cadastro/edição de serviço)

**Files:**
- Modify: `festly-frontend/src/components/ServicoForm.jsx`
- Modify: `festly-frontend/src/pages/EditarServico.jsx`

- [ ] **Step 1: Imports e schema no ServicoForm**

No topo de `ServicoForm.jsx`, adicionar imports:

```js
import { Loader2 } from 'lucide-react';
import { formatCep } from '@/lib/formatCep';
import { useBuscaCep } from '@/hooks/useBuscaCep';
```

No `schema` (objeto Zod), adicionar após `cidade`:

```js
  cidade: z.string().min(1, 'Cidade é obrigatória'),
  cep: z.string().min(8, 'CEP inválido'),
  estado: z.string().length(2, 'UF inválida'),
```

- [ ] **Step 2: Expor setValue e o hook**

Na desestruturação do `useForm`, adicionar `setValue`:

```js
  const {
    register,
    handleSubmit,
    control,
    setValue,
    formState: { errors },
  } = useForm({
    resolver: zodResolver(schema),
    defaultValues: defaultValues ?? { disponivel: true },
  });

  const { buscar, loading: buscandoCep, erro: erroCep } = useBuscaCep();

  function preencherEndereco(end) {
    setValue('cidade', end.cidade, { shouldValidate: true });
    setValue('estado', end.estado, { shouldValidate: true });
  }
```

- [ ] **Step 3: Substituir o bloco "Cidade" por CEP + Cidade/UF**

Localizar o bloco atual (`{/* Cidade */}` … até o fechamento da div) e substituir por:

```jsx
      {/* CEP */}
      <div className="space-y-1.5">
        <label className="text-sm font-medium">CEP</label>
        <div className="relative">
          <Controller
            name="cep"
            control={control}
            render={({ field }) => (
              <Input
                placeholder="00000-000"
                maxLength={9}
                value={field.value ?? ''}
                onChange={(e) => {
                  const masked = formatCep(e.target.value);
                  field.onChange(masked);
                  buscar(masked, preencherEndereco);
                }}
                onBlur={() => buscar(field.value, preencherEndereco)}
              />
            )}
          />
          {buscandoCep && (
            <Loader2 className="h-4 w-4 animate-spin absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground" />
          )}
        </div>
        {erroCep && <p className="text-xs text-destructive">{erroCep}</p>}
        {errors.cep && <p className="text-xs text-destructive">{errors.cep.message}</p>}
      </div>

      {/* Cidade + UF */}
      <div className="grid grid-cols-[1fr,80px] gap-4">
        <div className="space-y-1.5">
          <label className="text-sm font-medium">Cidade</label>
          <Input placeholder="Ex: Mossoró" {...register('cidade')} />
          {errors.cidade && <p className="text-xs text-destructive">{errors.cidade.message}</p>}
        </div>
        <div className="space-y-1.5">
          <label className="text-sm font-medium">UF</label>
          <Input maxLength={2} placeholder="RN" {...register('estado')} />
          {errors.estado && <p className="text-xs text-destructive">{errors.estado.message}</p>}
        </div>
      </div>
```

- [ ] **Step 4: Incluir cep/estado no defaultValues do EditarServico**

Em `EditarServico.jsx`, no `setDefaultValues({...})` (~linha 27-35), adicionar:

```js
        setDefaultValues({
          nome: data.nome,
          preco: Number(data.preco),
          tipoCobranca: data.tipoCobranca,
          categoria: data.categoria,
          cidade: data.cidade,
          cep: data.cep ?? '',
          estado: data.estado ?? '',
          descricao: data.descricao ?? '',
          disponivel: data.disponivel,
        });
```

(O `ServicoWizard` repassa `...values`, então cep/estado fluem sem alteração.)

- [ ] **Step 5: Lint do frontend**

Run: `cd festly-frontend; npm run lint`
Expected: sem erros nos arquivos alterados.

- [ ] **Step 6: Verificação manual**

Rodar `npm run dev`, ir em "Novo serviço", digitar um CEP válido (ex: `59600-000`) → "Cidade" e "UF" preenchem; CEP inválido → mensagem de erro. Editar um serviço existente carrega os campos.

- [ ] **Step 7: Commit**

```bash
git add festly-frontend/src/components/ServicoForm.jsx festly-frontend/src/pages/EditarServico.jsx
git commit -m "feat: busca por CEP no cadastro/edicao de servico"
```

---

## Task 7: Frontend — SolicitarAgendamentoModal

**Files:**
- Modify: `festly-frontend/src/components/SolicitarAgendamentoModal.jsx`

- [ ] **Step 1: Imports e hook**

Adicionar aos imports do topo:

```js
import { formatCep } from '@/lib/formatCep';
import { useBuscaCep } from '@/hooks/useBuscaCep';
```

`Loader2` já é importado. Dentro do componente, junto aos outros hooks de estado (após `const [salvando, setSalvando] = useState(false);`):

```js
  const { buscar: buscarCepEndereco, loading: buscandoCep, erro: erroCep } = useBuscaCep();
  const numeroRef = useRef(null);

  function preencherEndereco(end) {
    setRua(end.rua);
    setBairro(end.bairro);
    setCidade(end.cidade);
    setEstado(end.estado);
    numeroRef.current?.focus();
  }
```

(`useRef` já é importado no topo do arquivo.)

- [ ] **Step 2: Ligar o ref ao input "Número"**

Localizar o input de Número (~linha 397) e adicionar o `ref`:

```jsx
                    <Input ref={numeroRef} value={numeroEnd} onChange={(e) => setNumeroEnd(e.target.value)} className="h-8 text-xs" placeholder="123" />
```

- [ ] **Step 3: Substituir o campo CEP**

Localizar o bloco do CEP (~linha 418-421) e substituir por:

```jsx
                <div className="w-40">
                  <label className="text-[10px] font-medium text-muted-foreground uppercase tracking-wide block mb-1">CEP *</label>
                  <div className="relative">
                    <Input
                      value={cep}
                      onChange={(e) => {
                        const masked = formatCep(e.target.value);
                        setCep(masked);
                        buscarCepEndereco(masked, preencherEndereco);
                      }}
                      onBlur={() => buscarCepEndereco(cep, preencherEndereco)}
                      className="h-8 text-xs pr-7"
                      placeholder="00000-000"
                      maxLength={9}
                    />
                    {buscandoCep && (
                      <Loader2 className="h-3.5 w-3.5 animate-spin absolute right-2 top-1/2 -translate-y-1/2 text-muted-foreground" />
                    )}
                  </div>
                  {erroCep && <p className="text-[10px] text-destructive mt-1">{erroCep}</p>}
                </div>
```

- [ ] **Step 4: Lint do frontend**

Run: `cd festly-frontend; npm run lint`
Expected: sem erros no arquivo alterado.

- [ ] **Step 5: Verificação manual**

No catálogo, abrir "Solicitar agendamento" → passo 2 → "Novo" endereço → digitar CEP válido preenche Rua/Bairro/Cidade/UF e foca em "Número". CEP inválido mostra erro; campos seguem editáveis.

- [ ] **Step 6: Commit**

```bash
git add festly-frontend/src/components/SolicitarAgendamentoModal.jsx
git commit -m "feat: busca por CEP no modal de agendamento"
```

---

## Task 8: Frontend — Checkout (CEP de cobrança)

**Files:**
- Modify: `festly-frontend/src/pages/Checkout.jsx`

- [ ] **Step 1: Imports, hook e Controller**

Adicionar imports no topo:

```js
import { Loader2 } from 'lucide-react';
import { formatCep } from '@/lib/formatCep';
import { useBuscaCep } from '@/hooks/useBuscaCep';
import { Controller } from 'react-hook-form';
```

Na desestruturação do `useForm`, adicionar `control`:

```js
  const { register, handleSubmit, control, formState: { errors } } = useForm({
    resolver: zodResolver(cartaoSchema),
  });

  const { buscar: validarCep, loading: buscandoCep, erro: erroCep } = useBuscaCep();
```

- [ ] **Step 2: Trocar o campo CEP por máscara + validação**

Localizar o `Field label="CEP"` (~linha 180-182) e substituir o `<Input>` interno por um `Controller` que aplica máscara e valida a existência do CEP (sem campos visíveis para preencher):

```jsx
                <Field label="CEP" error={errors.cep}>
                  <div className="relative">
                    <Controller
                      name="cep"
                      control={control}
                      render={({ field }) => (
                        <Input
                          inputMode="numeric"
                          placeholder="00000-000"
                          maxLength={9}
                          value={field.value ?? ''}
                          onChange={(e) => {
                            const masked = formatCep(e.target.value);
                            field.onChange(masked);
                            validarCep(masked, () => {});
                          }}
                          onBlur={() => validarCep(field.value, () => {})}
                        />
                      )}
                    />
                    {buscandoCep && (
                      <Loader2 className="h-4 w-4 animate-spin absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground" />
                    )}
                  </div>
                  {erroCep && <p className="text-xs text-destructive">{erroCep}</p>}
                </Field>
```

- [ ] **Step 3: Lint do frontend**

Run: `cd festly-frontend; npm run lint`
Expected: sem erros no arquivo alterado.

- [ ] **Step 4: Verificação manual**

No checkout com cartão, digitar CEP inválido → mensagem "CEP não encontrado"; CEP válido → sem erro; máscara aplicada enquanto digita.

- [ ] **Step 5: Commit**

```bash
git add festly-frontend/src/pages/Checkout.jsx
git commit -m "feat: mascara e validacao de CEP no checkout"
```

---

## Self-review checklist (já verificado)

- **Cobertura do spec:** unidades compartilhadas (Tasks 3-5), 3 integrações (Tasks 6-8), backend cep/estado (Tasks 1-2), tratamento de erros (no hook + inline em cada form). ✓
- **Sem placeholders:** todo passo traz código real. ✓
- **Consistência de tipos:** `buscarCep`/`soDigitos`/`CepNaoEncontradoError` (cepService) usados pelo `useBuscaCep`; `useBuscaCep` retorna `{ buscar, loading, erro }`, consumido igual nos 3 forms; `formatCep` idêntico em todos. ✓
- **Sem testes:** conforme decisão do usuário. ✓
```
